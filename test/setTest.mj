program TestAddMethod

set my_set;
int a;
{
    void main() {
    
        // Create a new set with a capacity of 4
        my_set = new set[4];
      	a = 1;  
        // --- Test adding elements ---
        add(my_set, 10);
        add(my_set, 11);
        add(my_set, 12);
        print(my_set[0]);
        print(my_set[1]);
        print(my_set[2]);
        print(my_set[3]);
    }
}